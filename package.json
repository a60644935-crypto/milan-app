{
  "name": "milan-app",
  "version": "1.0.0",
  "private": true,
  "description": "Milan community app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "validate": "node --check server.js && node --check script.js && node --check admin.js"
  },
  "engines": {
    "node": ">=20"
  }
}
